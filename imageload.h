#ifndef IMAGELOAD_H
#define IMAGELOAD_H


/*
#include <string>
#include <SDL_image.h>


using namespace  std;

const string IMAGES = "/home/paul/workspace/GLFrameWork/images/system/";

const string IMG_ERROR = IMAGES + "errorAlpha.png";


class ImageLoad
{
public:
    ImageLoad();
    ImageLoad(string path);
    ~ImageLoad();
    static SDL_Surface * getSurface(string  path);

private:
    string _Path;
    static SDL_Surface * loadStandardSurface();  // loads a "error" image to avoid crash in opengl
};
*/
#endif // IMAGELOAD_H
